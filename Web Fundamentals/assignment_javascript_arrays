var arr = [1,"apple",-3,"orange",0.5]

function numbersOnly(arr){
 var arr_new = []
 for(x = 0; x < arr.length; x++){
    if(typeof(arr[x]) == 'number'){
      arr_new.push(arr[x])
  }
}
      console.log(arr_new)
}


numbersOnly(arr)

def draw_stars_and_names(arr):
    idx = 0
    while idx < len(arr):
        if arr[idx] != " ":
            print arr[idx] * '*'
        else arr[idx] ==
        idx += 1

draw_stars_and_names([4,"Tom",6,"Ricardo",4])
